# سیستم صرافی با کتابخانه استاندارد پایتون اجرا می‌شود.
# وابستگی‌های زیر اختیاری‌اند و فقط برای قابلیت‌های پیشرفته لازم می‌شوند.

# --- دیتابیس PostgreSQL (اختیاری؛ پیش‌فرض SQLite است) ---
psycopg2-binary>=2.9

# --- OCR سریال اسکناس (اختیاری؛ نیازمند tesseract سیستم) ---
pytesseract>=0.3
pillow>=9.0

# --- تشخیص تصویری اسکناس (چند اسکناس در یک عکس، آفلاین) ---
numpy>=1.26
opencv-python-headless>=4.9

# --- خروجی PDF فارسی (RTL با فونت وزیرمتن) ---
reportlab>=4.0
arabic_reshaper>=2.1
python-bidi>=0.4

# --- خروجی اکسل (xlsx) ---
openpyxl>=3.1

# --- توسعه و تست ---
pytest>=7.0
matplotlib>=3.5
